{
  "name": "spin-layers",
  "version": "1.0.0",
  "description": "SPIN Protocol Lambda Layers Project",
  "scripts": {
    "deploy-spin-common-libs": "cd spin-common-libs && ./deploy.sh",
    "remove-spin-common-libs": "cd spin-common-libs && sls remove --stage $STAGE",
    "local": "cd spin-common-libs && ./local.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Peter Jeong"
}
